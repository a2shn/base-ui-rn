import { RadioIndicator } from './indicator';
import { RadioRoot } from './radio';
import { RadioGroup } from './radio-group';

export const Radio = {
  Indicator: RadioIndicator,
  Root: RadioRoot,
};

export { RadioGroup, RadioIndicator, RadioRoot };
export * from './types';
