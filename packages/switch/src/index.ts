import { SwitchRoot } from './switch';
import { SwitchThumb } from './thumb';

export const Switch = {
  Root: SwitchRoot,
  Thumb: SwitchThumb,
};

export { SwitchRoot, SwitchThumb };
export * from './types';
